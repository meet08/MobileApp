<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="Assignment.Views.LoginPage"
             BackgroundColor="{DynamicResource GenericBackgroundColor}"
             Title="Login Page">

    <StackLayout VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" Padding="10,30,10,10" Spacing="10">
        <Entry Text="{Binding UserName}" Placeholder="User Name" HorizontalTextAlignment="Center" BackgroundColor="Black" WidthRequest="275" HeightRequest="50" TextColor="White" HorizontalOptions="CenterAndExpand" PlaceholderColor="White" ></Entry>
        <Button Text="Ok" Command="{Binding LoginCommand}" HeightRequest="50" TextColor="Black" BackgroundColor="LightGray" WidthRequest="280" HorizontalOptions="Center" FontSize="Medium"></Button>
    </StackLayout>
  
</ContentPage>